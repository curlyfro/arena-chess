{
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.1.0"
  },
  "lint-staged": {
    "frontend/src/**/*.{ts,tsx}": "cd frontend && npx eslint --fix --no-warn-ignored"
  }
}
